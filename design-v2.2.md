# WISE INVESTOR SYSTEM
## 현명한 장기투자자 AI 시스템 — 아키텍처 설계 문서 v2.2

> **v2.2 변경사항:** 3차 검토를 반영하여 Phase 1을 **완전 로컬·비용 0원**으로 재설계하였습니다.
> 외부 LLM API는 **로컬 구현이 품질 기준을 충족하지 못했을 때의 최후 수단**으로 위상을 재조정했습니다.
> "다른 LLM" 원칙은 Ollama의 **서로 다른 로컬 모델**(Llama 3.1 8B + Qwen 2.5 7B)로 확보합니다.
> Phase 1에 **수동 밸류체인 문서**와 **품질 측정 지표**를 추가하여 Go/No-Go 판단의 신뢰성을 높였습니다.
> EDGAR 파싱을 Phase 3에서 "FMP 서비스 중단 시의 최종 대체재(진실의 근원)"로 전략적 승격하였습니다.
>
> **v2.1 변경사항:** 2차 전문가 리뷰를 반영하여 yfinance 중단 리스크 대응, EDGAR 파싱 난이도 명시,
> "LLM은 판단/Python은 계산" 원칙 추가, 에이전트 순차 처리 큐, 토큰 비용 하드 리밋,
> 디베이트 컨텍스트 압축, 밸류체인 연쇄 비용 통제를 반영하였습니다.
>
> **v2.0 변경사항:** 1차 전문가 리뷰를 반영하여 데이터 품질 검증 체계, DCF 의존도 조정,
> 디베이트 실효성 강화 방안, 사전 필터 현실화, 하드웨어 한계 대응, 구축 일정 현실화를 전면 수정하였습니다.

> 이 문서는 멀티 에이전트 AI 기반의 펀더멘털 장기투자 분석 시스템의 전체 설계를 설명합니다.
> 기술적 배경 지식이 없는 사람도 이해할 수 있도록 작성되었습니다.

---

# 1. 시스템 개요: 이것은 무엇인가?

이 시스템은 한마디로 **"감정에 휘둘리지 않는 현명한 장기투자자"를 AI로 만드는 프로젝트**입니다.

실제 투자 결정을 대신 내려주는 로봇이 아닙니다. 투자자가 더 나은 판단을 내릴 수 있도록 도와주는 **"분석 비서"**입니다. 마치 워렌 버핏이나 하워드 막스 같은 현명한 투자자가 옆에 앉아서 "이 기업을 사야 하는 이유"와 "사지 말아야 하는 이유"를 동시에 분석해서 보고서로 정리해주는 것과 같습니다.

### 핵심 철학

- **차트 분석 제로:** 주가 차트나 기술적 지표를 일체 사용하지 않습니다. 오직 기업의 본질적 가치만 봅니다.
- **편향 제거:** "이 주식이 좋다"는 의견에 반드시 "이 주식이 위험한 이유"를 함께 분석합니다. 이것을 "레드팀(Red Team)" 접근법이라고 합니다.
- **장기 관점:** 다음 주 주가 예측이 아니라, "이 기업이 5년, 10년 후에도 돈을 잘 벌 수 있는가?"를 판단합니다.
- **인간이 최종 결정:** AI는 분석과 보고서를 제공하고, 최종 투자 판단은 반드시 사람이 합니다.
- **로컬 우선(v2.2 신규):** Phase 1은 외부 LLM API 비용 0원으로 구축합니다. 로컬 모델 다양성으로 "다른 LLM" 원칙을 확보하며, 외부 API는 로컬이 품질 기준을 통과하지 못했을 때의 **최후 수단**으로만 도입합니다.

### 이런 시스템이 이미 존재하는가?

**정확히 이 형태로는 존재하지 않습니다.** 헤지펀드들이 유사한 시스템을 내부적으로 사용하고 있을 가능성은 높지만 공개하지 않습니다. 오픈소스 프로젝트(TradingAgents 등)가 일부 요소를 구현했지만, 일일 트레이딩 중심이라 장기투자 관점과 다릅니다. 상용 SaaS 도구(FinChat, Gainify 등)는 "질문하면 답해주는" 수동 도구이지, 알아서 정보를 모니터링하고 종합 판단까지 내려주는 시스템이 아닙니다.

2025~2026년에 구성 요소들(AI 모델 성능, 에이전트 프레임워크, 무료 데이터 API)이 모두 성숙하면서 **지금 처음으로 개인이 만들 수 있는 수준이 되었습니다.**

---

# 2. 비유로 이해하기: 가상의 투자 회사

이 시스템을 이해하는 가장 쉬운 방법은, **1인 투자 회사**를 상상하는 것입니다.

당신이 사장이고, AI 직원 5명이 각자 전문 분야를 가지고 일합니다. 여기에 정보를 수집하는 인턴과, 외부 정보를 전달해주는 비서가 추가됩니다.

| 역할 | 비유 | 하는 일 |
|------|------|---------|
| **OpenClaw** | 개인 비서 | 주식 모임 카톡방 정보를 받아서 회사에 전달하고, 분석 결과를 나에게 알려줌 |
| **데이터 수집기** | 신문 구독 + 공시 감시 인턴 | 24시간 뉴스, 공시, 경제지표를 자동으로 모니터링 |
| **사전 필터** | 우편물 분류 담당자 | 쏟아지는 정보 중 "우리 관심 종목과 관련 있는 것"만 골라냄 |
| **The Economist** | 거시경제 전문가 | 금리, 인플레이션, 지정학적 리스크 등 큰 그림을 읽음 |
| **The Analyst** | 기업 분석가 | 기업의 재무제표, 경쟁우위(moat), 사업 모델을 분석 |
| **The Valuer** | 가치평가 전문가 | 멀티플 비교와 역DCF로 가격의 적정성을 판단 |
| **The Skeptic** | 악마의 변호인 | 위 세 사람의 분석에 반론을 제기. "틀렸다면 어디서 틀렸나?" |
| **The Steward** | 최종 결정자(사장 보좌관) | 모든 의견을 종합하여 Buy/Hold/Pass 판단과 투자 비중을 제안 |

실제 투자 회사에서도 이런 구조로 의사결정을 합니다. 이 시스템은 그 과정을 AI로 자동화한 것입니다.

---

# 3. 정보의 흐름: 데이터는 어떻게 들어오는가?

시스템에 정보가 들어오는 경로는 **두 갈래**입니다.

## 3.1. 경로 1 — 사람을 통한 정보 (OpenClaw)

OpenClaw는 오픈소스 AI 개인 비서입니다. 텔레그램, 카카오톡 등 메시징 앱과 연결되어 24시간 작동합니다.

**작동 방식:**

1. 주식 모임 카톡방에서 누군가 "요즘 TSMC 실적이 좋다던데"라고 말함
2. 내가 그 메시지를 OpenClaw에게 포워딩 (또는 직접 "TSMC 분석해줘"라고 명령)
3. OpenClaw가 이 정보를 뒤쪽 분석 시스템에 전달
4. 분석이 끝나면 OpenClaw가 나에게 결과를 텔레그램으로 알려줌

**핵심 포인트:** OpenClaw는 "뇌"가 아니라 "신경계"입니다. 외부 자극(정보)을 받아서 뇌(분석 엔진)로 전달하고, 뇌의 판단을 다시 나에게 전달하는 역할만 합니다. 따라서 OpenClaw에 증권사 API 키 같은 민감한 정보를 줄 필요가 없어 보안 리스크가 낮습니다.

## 3.2. 경로 2 — 시스템이 직접 수집하는 정보 (자동 모니터링)

분석 엔진 안의 데이터 수집기가 자동으로 다음 소스들을 감시합니다.

### 속보/뉴스 소스

| 소스 | 내용 | 비용 |
|------|------|------|
| **세이브티커(SaveTicker)** | 한국어로 큐레이션된 미국 증시 실시간 뉴스, 속보, 경제지표 | 무료 앱 |
| **Google News RSS** | 관심 종목 키워드 기반 영문 뉴스 자동 수집 | 무료 |
| **Reddit (r/investing 등)** | 시장 센티먼트, 개인 투자자 분위기 파악 | 무료 |

### 공시/규제 소스

| 소스 | 내용 | 비용 |
|------|------|------|
| **SEC EDGAR** | 미국 상장 기업의 10-K(연간보고서), 10-Q(분기보고서), 8-K(수시공시) | 무료 API |
| **DART 전자공시** | 한국 상장 기업 공시 (한국 주식 보유 시) | 무료 API |
| **FOMC 의사록** | 미국 연방준비제도의 금리 결정 및 경제 전망 | 무료 |

### 거시경제/지정학 소스

| 소스 | 내용 | 비용 |
|------|------|------|
| **FRED API** | 미국 연준 제공 경제 데이터 — 금리, CPI(물가), 실업률, GDP 등 | 무료 (키 발급) |
| **GPR Index** | 학술적으로 검증된 지정학적 리스크 지수 (전쟁, 테러, 무역 분쟁 등) | 무료 |
| **USD/KRW 환율** | 한국에서 미국 주식 투자 시 필수 모니터링 항목 | 무료 |

### 기업 재무 소스

| 소스 | 내용 | 비용 |
|------|------|------|
| **Financial Modeling Prep** | 주력 재무 데이터 소스 (상세 재무제표, 비율 분석) | 무료(일 250건) 또는 유료(월 $14) |
| **yfinance** | 보조/백업 소스 (주가, 배당, 기본 재무) | 무료 |
| **Earnings Whispers** | 어닝콜(실적 발표) 일정 및 시장 예상치 | 무료 (일부) |

### ⚠️ 무료 데이터의 현실적 한계와 대응 전략

#### yfinance의 근본적 불안정성

yfinance는 공식 API가 아닙니다. 야후 파이낸스 웹사이트를 자동으로 긁어오는(스크래핑) 방식이기 때문에, **야후가 웹페이지 구조를 바꾸면 즉시 작동을 멈춥니다.** 실제로 과거에 여러 차례 이런 일이 발생했습니다. 투자 결정이 내려지는 핵심 순간에 데이터가 누락될 리스크가 큽니다.

이 때문에 yfinance를 "주력 데이터 소스"로 사용하는 것은 위험합니다.

**수정된 데이터 소스 전략:**

| 역할 | 소스 | 이유 |
|------|------|------|
| **주력 (Primary)** | Financial Modeling Prep | 공식 API, 안정적, 체계적 재무 데이터 |
| **백업 (Fallback)** | yfinance | 주력 소스 장애 시 또는 교차검증용 |
| **원본 (Ground Truth)** | SEC EDGAR 원본 Filing | 최종 검증용. 모든 수치의 진실의 근원 |

Financial Modeling Prep(FMP) 무료 티어(일 250건)로 시작하되, 종목이 늘어나면 Starter 플랜(월 $14, 약 1.8만원, 일 750건)으로 업그레이드합니다. **단, Phase 1은 종목 1개에 집중하므로 무료 티어로 충분합니다.**

#### SEC EDGAR 파싱의 실제 난이도

SEC EDGAR에서 10-K/10-Q를 무료로 가져올 수 있다고 해서 "쉽다"는 뜻은 아닙니다. 이 문서들은 복잡한 HTML과 XBRL(재무 데이터용 태그 언어)로 되어 있어서, AI가 읽을 수 있는 깔끔한 텍스트로 정제(Parsing)하는 과정에 **상당한 예외 처리 로직**이 필요합니다.

기업마다 Filing 형식이 다르고, 표의 구조가 제각각이며, 각주와 본문이 뒤섞여 있습니다. "10-K를 읽어서 분석한다"는 말이 간단해 보이지만, 실제로는 파싱 파이프라인 자체가 하나의 소프트웨어 프로젝트입니다.

#### ⚠️ v2.2 수정: EDGAR의 전략적 위상 재정의

v2.1에서는 EDGAR를 "교차검증용 원본"으로만 배치했습니다. v2.2에서는 **EDGAR를 FMP 서비스 중단 시의 최종 대체재로 승격**합니다.

**이유:** FMP는 외부 상용 서비스입니다. 가격 인상, 무료 티어 축소, 서비스 종료 중 어느 하나라도 발생하면 전 시스템이 흔들립니다. 장기적 관점에서 "진실의 근원(ground truth)"은 FMP가 아닌 **미국 증권거래위원회가 법적으로 관리하는 EDGAR**여야 합니다.

**단계적 도입:**

- **Phase 1~2:** FMP를 편의 레이어로 사용. EDGAR는 텍스트 분석(Risk Factors, MD&A, 사업 개요)에만 활용. 재무 수치 파싱은 시도하지 않음
- **Phase 3:** edgartools 등 전문 라이브러리로 **XBRL 태그 기반 수치 파싱**을 본격 구현. HTML 본문 자유 텍스트는 파싱 시도하지 않고 XBRL 정형 데이터만 추출
- **Phase 3 이후:** FMP를 대체 가능한 편의 레이어로 재정의. FMP 장애 시 EDGAR 파이프라인이 전 시스템을 지탱

#### Phase 1 교차검증 파이프라인 (경량)

⚠️ **v2.2 수정:** v2.1은 Phase 1에서 FMP↔EDGAR 수치 교차검증을 요구했으나, EDGAR 파싱이 Phase 3 작업임을 인정하여 Phase 1 교차검증을 **경량 버전**으로 축소합니다.

| 데이터 항목 | 1차 소스 | 2차 소스 | 불일치 시 처리 |
|-------------|----------|----------|----------------|
| 주가, 시가총액 | FMP API | yfinance | 5% 초과 시 플래그 |
| 기본 재무 비율(PER, 부채비율) | FMP API | yfinance | 5% 초과 시 플래그 |
| 어닝콜 트랜스크립트 | Seeking Alpha | SEC EDGAR 8-K 첨부 | 둘 다 수집, 대조 |

⚠️ **Phase 1 한계 고지:** FMP와 yfinance는 모두 야후/제3자 소스에서 파생되므로 **완전 독립 검증이 아닙니다.** 투자자는 Tier 1 종목의 핵심 수치를 **수동으로 10-K 원본에서 재확인**해야 합니다. 이 한계는 Phase 3 EDGAR XBRL 파싱 도입으로 해소됩니다.

#### Phase 3 정밀 교차검증 파이프라인

| 데이터 항목 | 1차 소스 | 2차 소스 | 불일치 시 처리 |
|-------------|----------|----------|----------------|
| 매출, 순이익, EPS | FMP API | SEC EDGAR XBRL | EDGAR 우선 채택 |
| 부채비율, FCF | FMP API | SEC EDGAR XBRL | EDGAR 우선 채택 |
| 주가, 시가총액 | FMP API | yfinance | 실시간 기준 우선 |

**불일치가 감지되면:** 보고서에 "⚠️ 데이터 소스 간 불일치 감지"를 명시하고, 어떤 소스의 수치를 사용했는지 근거를 함께 제시합니다.

**API 호출 한도 관리:** FMP 무료 티어의 일 250건 제한을 초과하지 않도록, Tier 1 종목 심층 분석을 요일별로 분산 실행합니다. 긴급 분석(공시 발생 등)을 위한 예비 호출량(일 50건)을 항상 확보해둡니다.

---

# 4. 사전 필터: 노이즈를 걸러내는 첫 번째 관문

위의 모든 소스에서 하루에 수천 건의 뉴스와 데이터가 쏟아집니다. 이것을 전부 AI에게 분석시키면 비용이 폭발합니다.

### 3단계 점진적 필터 전략

"브라질 커피 가격 상승"이 스타벅스 보유자에게는 관련이 있고, "일본 엔화 약세"가 반도체 장비 투자에 간접적으로 영향을 줄 수 있습니다. 이런 2차, 3차 연결을 작은 AI 모델이 자동 판단하기는 현실적으로 어렵습니다. 따라서 필터를 처음부터 완벽하게 만들려 하지 않고, 단계적으로 고도화합니다.

**1단계 (초기 — 보수적, 느슨한 필터):**
- 직접 종목명/티커 언급만 필터링 (키워드 매칭, AI 불필요)
- 예: "NVIDIA", "NVDA", "엔비디아" 등이 포함된 뉴스만 통과
- 오탐(false positive)이 많더라도 놓치는 것(false negative)을 최소화
- 비용: 0원 (단순 문자열 매칭)

**2단계 (안정화 후 — 밸류체인 키워드 매칭 추가):**
- 밸류체인 그래프에 등록된 기업명도 키워드에 포함
- 예: NVIDIA 밸류체인에 TSMC가 있으므로 "TSMC" 뉴스도 자동 통과
- 여전히 단순 키워드 기반이라 비용 0원

**3단계 (고도화 — 로컬 AI 의미 필터링):**
- 1~2단계를 통과한 뉴스에 대해서만 로컬 8B 모델이 "이것이 실제로 중요한 변화인가?" 판단
- 전체 뉴스가 아니라 이미 걸러진 소수에만 AI를 적용하므로 비용 통제 가능
- 로컬 실행이므로 추가 비용 0원
- 2~3차 연결("브라질 커피 → 스타벅스 원가")은 이 단계에서 점진적으로 학습

**핵심 원칙:** 초기에는 필터를 느슨하게 설정하고 점진적으로 조입니다. 중요한 정보를 놓치는 것이 불필요한 정보를 보는 것보다 훨씬 위험합니다.

---

# 5. 지식 레이어: 시스템의 기억 장치

필터를 통과한 정보는 세 가지 저장소에 축적됩니다.

## 5.1. 밸류체인 그래프 (Value Chain Graph)

이것이 이 시스템의 가장 독특하고 강력한 기능입니다.

**밸류체인이란?** 하나의 기업이 사업을 하기 위해 연결된 다른 기업들의 관계망입니다.

예를 들어 NVIDIA를 분석한다면:

- **상류(공급업체):** TSMC(반도체 제조), SK하이닉스(HBM 메모리), ASML(노광장비)
- **동급(경쟁사):** AMD, Intel, Broadcom
- **하류(고객사):** Microsoft Azure, AWS, Google Cloud
- **인프라:** 전력회사, 냉각장비 업체 (데이터센터 관련)

시스템은 이 관계를 **그래프(네트워크) 형태**로 저장합니다. 기술적으로는 Python의 NetworkX 라이브러리와 JSON 파일을 사용하며, 비용은 0원입니다.

### ⚠️ v2.2 수정: 단계적 구축 전략

**밸류체인 기능은 이 시스템의 핵심 차별점(§11.3 "밸류체인 사고 자동화")입니다.** 따라서 완전히 자동화된 그래프가 Phase 2에 들어간다 하더라도, **Phase 1 MVP에서 밸류체인 기능이 전혀 없으면 "이 시스템이 가치 있는가?"를 판단할 수 없습니다.** 결과적으로 Go/No-Go 판단이 오염됩니다.

**해결책: Phase 1에 수동 밸류체인 문서를 포함합니다.**

- **Phase 1 — 수동 밸류체인 문서:**
  - 형식: Markdown 또는 Notion 페이지. 구조화된 JSON/NetworkX는 아직 없음
  - 내용: 분석 대상 종목 1개에 대해 상류·동급·하류·인프라를 5~10개 항목 수준으로 **사람이 직접 작성**
  - 활용: Analyst 에이전트의 RAG 컨텍스트에 포함. 프롬프트에 "이 기업의 밸류체인은 다음과 같다. 분석에 반영하라"로 주입
  - 평가 기준: Phase 1 MVP 종료 시점에 "밸류체인 정보가 보고서 품질을 유의미하게 개선했는가?"를 별도 질문으로 평가
- **Phase 2 — 자동 구축 그래프:**
  - AI가 해당 기업의 10-K와 어닝콜을 읽고 밸류체인을 자동 추출
  - Phase 1의 수동 문서가 **자동 추출의 정답지(ground truth)** 로 재활용됨. 자동 추출 정확도를 측정 가능
  - ⚠️ 초기 자동 추출 정확도는 낮을 수 있으므로 사람의 수동 보정을 항상 병행
- **Phase 3 — 자동 업데이트:**
  - 뉴스 모니터링 중 밸류체인 변화 신호가 감지되면 자동 업데이트
  - 예: "TSMC, 미국 애리조나 공장 가동 시작" → NVIDIA 밸류체인의 지정학 리스크 항목 업데이트
  - 예: "SK하이닉스 HBM4 양산 발표" → NVIDIA 공급 역량 항목 업데이트
- **Phase 3 — 연쇄 알림 (가장 강력한 기능):**
  - 내 관심 종목이 아니더라도, 관심 종목의 밸류체인에 속한 기업에 중요한 뉴스가 뜨면 연결해서 알려줌

### 밸류체인 연쇄 비용 통제

연쇄 알림은 강력하지만, 통제하지 않으면 비용 폭탄이 됩니다. 하나의 뉴스(예: "미국 대중국 반도체 규제 강화")가 밸류체인을 타고 10개 이상의 종목에 재분석을 트리거할 수 있습니다.

**연쇄 비용 통제 규칙:**

- **일일 심층 분석 상한:** 하루 최대 3건의 Tier 1급 심층 분석만 실행. 초과 건은 다음 날로 이연
- **연쇄 깊이 제한:** 밸류체인 연쇄는 최대 2단계까지만 (예: TSMC → NVIDIA는 허용, TSMC → NVIDIA → Microsoft Azure는 허용하지 않고 Tier 2 모니터링으로 전환)
- **긴급도 분류:** 연쇄 알림에도 "긴급"과 "참고"를 구분. "긴급"(직접적 공급망 차질 등)만 즉시 분석, "참고"(간접적 영향)는 정기 리밸런싱 시점에 일괄 검토

## 5.2. RAG 저장소 (ChromaDB)

RAG는 "Retrieval-Augmented Generation"의 약자로, 쉽게 말하면 **AI가 참고할 수 있는 문서 도서관**입니다.

수집된 10-K 보고서, 어닝콜 트랜스크립트, 주요 뉴스 기사 등이 여기에 저장됩니다. 에이전트들이 분석할 때 이 도서관에서 관련 문서를 꺼내 읽고 근거로 활용합니다.

ChromaDB라는 오픈소스 벡터 데이터베이스를 사용하며, 비용은 0원입니다. 맥미니 로컬에서 돌아갑니다.

> ⚠️ **메모리 관리:** ChromaDB에 임베딩이 누적되면 메모리 사용량이 점진적으로 증가합니다. 6개월 이상 지난 일반 뉴스 임베딩은 주기적으로 정리(가비지 컬렉션)하되, 10-K/10-Q 같은 핵심 재무 문서와 어닝콜 트랜스크립트는 영구 보존합니다.

## 5.3. 포트폴리오 상태 (SQLite)

현재 보유 종목, 매수 가격, 과거 분석 이력, 에이전트들의 판단 기록 등을 저장합니다. SQLite라는 가벼운 데이터베이스를 사용하며, 비용은 0원입니다.

---

# 6. 3-Tier 종목 관리: 비용 효율적으로 많은 종목 커버하기

여러 종목을 보고 싶지만, 모든 종목에 동일한 깊이의 분석을 돌리면 비용이 급증합니다. 해결책은 **계층화**입니다.

> ⚠️ **Phase 1 주의:** 3-Tier 시스템은 Phase 3에서 구현됩니다. Phase 1 MVP는 **Tier 1 종목 1개**로만 시작하여 시스템의 가치를 검증합니다.

## Tier 1 — 핵심 보유 종목 (3~5개)

**가장 깊은 분석.** 3~5개 에이전트가 Bull/Bear 디베이트까지 전부 돌립니다.

- 분기 실적 발표 시 즉시 재분석
- 주요 뉴스 발생 시 재분석
- 멀티플 기반 적정가치 정기 업데이트
- **Phase 1 기준 비용 (로컬 모델):** 종목당 **0원** (전력 비용 외)
- **Phase 2 이후 API 도입 시:** 종목당 약 100~200원 (필요 시)
- API 호출 한도 관리를 위해 요일별 분산 실행

## Tier 2 — 관심 종목 + 밸류체인 핵심 기업 (10~20개)

**경량 모니터링.** 로컬 8B 모델이 뉴스와 공시만 필터링합니다.

- 일상적인 뉴스는 요약만 저장
- "이건 중요한 변화다" 싶을 때만 Tier 1 수준의 심층 분석 트리거
- 비용: 0원 (로컬 처리)

## Tier 3 — 밸류체인 확장 종목 (50~100개)

**패시브 감시.** AI 호출 없이 단순 키워드/RSS 필터링만 합니다.

- Tier 1, Tier 2 종목에 영향 있는 이벤트가 발생할 때만 활성화
- 예: ASML(Tier 3)에서 수출 제한 뉴스 → TSMC(Tier 2)와 NVIDIA(Tier 1)에 연쇄 알림
- 비용: 완전 0원 (키워드 매칭만)

### 비용 요약 (Phase 3 완성 시점)

| Tier | 종목 수 | 분석 깊이 | 월 비용 (로컬 전환 시) | 월 비용 (API 옵션) |
|------|---------|-----------|------------------------|--------------------|
| Tier 1 | 3~5개 | 5개 에이전트 풀 분석 | 0원 | 약 1,000~2,000원 |
| Tier 2 | 10~20개 | 로컬 모델 경량 모니터링 | 0원 | 0원 |
| Tier 3 | 50~100개 | 키워드 필터링만 | 0원 | 0원 |
| **합계** | **63~125개** | | **0원** | **약 1,000~2,000원/월** |

---

# 7. 분석 엔진: AI 전문가들

이 시스템의 심장부입니다. 여러 AI 에이전트가 각자의 전문 영역에서 분석하고, 토론하고, 최종 판단을 내립니다.

CrewAI라는 오픈소스 프레임워크를 사용하여, 각 에이전트에게 역할(Role), 목표(Goal), 배경 스토리(Backstory)를 부여합니다.

> ⚠️ **MVP 전략:** 전체 5개 에이전트를 한 번에 구현하지 않습니다. 먼저 **Analyst + Valuer + Skeptic 3개 에이전트**로 종목 1개의 분석 사이클을 완성합니다. "이 보고서가 정말 내 투자 판단에 도움이 되는가?"를 검증한 뒤에 Economist와 Steward를 추가합니다.

### 핵심 아키텍처 원칙 — "LLM은 판단, Python은 계산"

이 시스템에서 가장 중요한 설계 원칙입니다.

**LLM(AI 모델)은 텍스트 생성기이지 계산기가 아닙니다.** 할인율, 성장률, 밸류에이션 배수 등을 LLM에게 직접 계산시키면 치명적인 수치 오류를 범할 수 있습니다. 예를 들어 "NVIDIA의 EV/EBITDA를 계산해줘"라고 하면, LLM이 그럴듯하지만 틀린 숫자를 자신 있게 제시할 수 있습니다. 이것은 환각(hallucination)의 일종입니다.

**역할 분리 원칙:**

| 역할 | 담당 | 예시 |
|------|------|------|
| **판단, 해석, 논리 구성** | LLM (AI 에이전트) | "이 기업의 경쟁 우위는 네트워크 효과이다", "현재 밸류에이션이 역사적 상단에 있으므로 주의가 필요하다" |
| **수치 계산, 비율 산출, 데이터 조회** | Python 함수 (도구/Tool) | PER 계산, DCF 할인 연산, 동종업계 평균 산출, 재무 비율 계산 |

**작동 방식:** CrewAI에서는 에이전트에게 "도구(Tool)"를 부여할 수 있습니다. 예를 들어 The Valuer 에이전트에게 다음과 같은 Python 도구를 줍니다.

- `calculate_per(ticker)` → FMP API에서 데이터를 가져와 PER을 정확히 계산하여 반환
- `calculate_ev_ebitda(ticker)` → EV/EBITDA를 정확히 계산하여 반환
- `get_peer_multiples(ticker)` → 동종업계 기업들의 멀티플을 테이블로 반환
- `reverse_dcf(ticker, current_price)` → 현재 주가에 내재된 성장률을 역산

LLM은 이 도구들을 "호출"하여 정확한 숫자를 받아온 뒤, 그 숫자를 "해석"하고 "판단"하는 역할만 합니다. 계산은 한 건도 LLM이 직접 하지 않습니다.

**이 원칙이 적용되는 모든 에이전트:**

- **Analyst:** 재무 비율은 Python이 계산 → LLM은 "이 수치가 의미하는 바"를 해석
- **Valuer:** 멀티플, 역DCF는 Python이 계산 → LLM은 "이 밸류에이션이 적정한지" 판단
- **Skeptic:** Bull 측의 수치를 Python 도구로 재검증한 후 → LLM이 반론 논리를 구성
- **Economist:** 거시경제 지표는 FRED API에서 직접 조회 → LLM은 "이 지표가 시사하는 바"를 해석

## 7.1. The Economist — 세상을 읽는 자

> 📌 **구현 순서: Phase 2에서 추가** (MVP에는 미포함)

**역할:** 거시경제와 지정학 전문가. 레이 달리오의 "경제 기계" 프레임워크처럼, 지금 세상이 어떤 상황인지 큰 그림을 읽습니다.

**분석 항목:**

- **금리 사이클:** 지금 금리가 오르는 중인가, 내리는 중인가?
- **인플레이션:** 물가가 오르면 기업의 원가가 증가하고, 소비자 지출이 줄어들 수 있습니다.
- **지정학적 리스크:** 미중 무역 갈등, 대만해협 긴장, 관세 정책 변화 등.
- **산업 메가트렌드:** AI 인프라 투자 확대, 에너지 전환, 인구 고령화 등 5~10년 단위의 구조적 변화.
- **환율:** 한국에서 미국 주식에 투자할 때, 원달러 환율 변동은 수익률에 직접 영향을 줍니다.

**Python 도구:** `get_fred_data(indicator)`, `get_gpr_index()`, `get_exchange_rate()`

**데이터 소스:** FRED API, GPR Index, FOMC 의사록, 뉴스 분석

## 7.2. The Analyst — 사업을 꿰뚫는 자

> 📌 **구현 순서: Phase 1 MVP에 포함**

**역할:** 기업 분석 전문가. 재무제표 숫자 이전에 "이 회사가 왜 돈을 버는지"를 이해합니다.

**분석 항목:**

- **경쟁 우위(Moat):** 이 기업이 경쟁사보다 잘하는 것은 무엇인가? 10년 후에도 유지 가능한가?
- **재무 건전성:** 부채비율, 잉여현금흐름, 영업이익률 등 핵심 재무 지표
- **경영진 품질:** 어닝콜에서 경영진이 하는 말의 일관성, 자본 배분 능력
- **산업 내 위치:** 시장 점유율, 성장률, 경쟁사 대비 강점/약점
- **밸류체인 맥락(v2.2 신규):** Phase 1에서는 수동 밸류체인 문서를 RAG 컨텍스트로 받아 상류·동급·하류 관계를 분석에 반영

**Phase 1 LLM:** Llama 3.1 8B (로컬 Ollama)

**Python 도구:** `get_financial_statements(ticker)`, `calculate_ratios(ticker)`, `get_competitor_comparison(ticker)`

**데이터 소스:** FMP API(주력), SEC EDGAR(10-K, 10-Q 텍스트만), 어닝콜 트랜스크립트, RAG 저장소, **수동 밸류체인 문서**

> **데이터 검증 규칙:** Analyst가 인용하는 모든 재무 수치는 반드시 출처를 명시해야 합니다. "FMP 기준 매출 $XX" 또는 "10-K p.42 기준 영업이익률 XX%"처럼 적시하여, 투자자가 원본을 직접 확인할 수 있게 합니다. 출처 없는 수치는 보고서에 포함하지 않습니다.

## 7.3. The Valuer — 가격을 매기는 자

> 📌 **구현 순서: Phase 1 MVP에 포함**

**역할:** 가치평가 전문가. 사업이 아무리 좋아도 가격이 비싸면 좋은 투자가 아닙니다.

### 가치평가 방법론 (우선순위)

**1순위 — 멀티플 비교 (주력):**
- PER, EV/EBITDA, PBR을 동종업계 경쟁사와 비교
- 해당 기업의 과거 5~10년 멀티플 범위 대비 현재 위치 확인
- **모든 멀티플은 Python 도구가 계산**, LLM은 해석만 수행

**2순위 — 역DCF (보조):**
- "현재 주가에 내재된 시장의 성장 기대치가 얼마인가?"를 역산
- 예: "현재 NVIDIA 주가는 향후 5년간 연평균 매출 성장률 40%를 가정하고 있음. 이것이 현실적인가?"
- **역산 계산은 Python 함수가 수행**, LLM은 그 성장률의 현실성을 판단

**3순위 — 정방향 DCF (참고용으로만):**
- 낙관/보통/비관 3가지 시나리오를 병행
- 각 시나리오의 가정을 명시적으로 공개
- **LLM은 가정(성장률, 할인율)을 "제안"하고, Python이 계산을 수행**
- 투자자가 가정의 합리성을 직접 판단해야 함

**Phase 1 LLM:** Llama 3.1 8B (로컬 Ollama)

**Python 도구:** `calculate_per(ticker)`, `calculate_ev_ebitda(ticker)`, `get_historical_multiples(ticker, years)`, `get_peer_multiples(ticker)`, `reverse_dcf(ticker, price)`, `forward_dcf(ticker, growth_rate, discount_rate, terminal_growth)`

## 7.4. The Skeptic — 의심하는 자 (레드팀)

> 📌 **구현 순서: Phase 1 MVP에 포함 (옵션 A 로컬 모델 다양성)**

**역할:** 악마의 변호인. 위 에이전트들의 분석을 의도적으로 공격합니다.

### ⚠️ v2.2 핵심 변경: Phase 1 완전 로컬 구현

v2.1은 Skeptic의 "다른 LLM" 원칙을 외부 API(DeepSeek-R1)로 확보할 것을 제안했습니다. v2.2는 **이 원칙을 Ollama의 서로 다른 로컬 모델로 구현**합니다. Phase 1 API 비용이 0원으로 유지됩니다.

**Phase 1 Skeptic 구성:**

- **LLM:** Qwen 2.5 7B (또는 DeepSeek-R1-Distill-Qwen-7B 중 택1)
- **왜 이 모델들인가?**
  - Qwen 2.5: Alibaba가 개발. Llama(Meta)와 훈련 데이터·아키텍처가 독립적. **실질적 관점 다양성** 확보
  - DeepSeek-R1-Distill-Qwen-7B: 반박·추론 능력에 특화된 증류 모델. Skeptic 역할에 적합
- **모델 로딩 전략:** Ollama에 Llama와 Qwen을 사전 다운로드. 순차 처리 큐가 Analyst/Valuer 종료 후 Llama 언로드 → Skeptic 차례에 Qwen 로드
- **스왑 오버헤드:** 30초~2분. Phase 1은 일 1~3건 분석이라 무시 가능
- **비용: 0원**

**Phase 2 업그레이드 경로:**

1. 로컬 모델이 Skeptic 품질 기준(§10.2 품질 지표)을 통과 → Qwen을 더 큰 모델(Qwen 2.5 14B, Llama 3.3 70B 양자화)로 업그레이드
2. 로컬 모델로 품질 기준 미달 → **그때 비로소** DeepSeek-R1 API를 Skeptic 한정으로 도입 검토

### 디베이트 실효성을 위한 3가지 강화 방안

**강화 1 — Skeptic 전용 데이터 소스 주입:**

Skeptic에게는 다른 에이전트들이 보지 않는 별도 데이터를 제공합니다.
- 공매도 리포트 (Hindenburg Research, Muddy Waters 등의 공개 보고서)
- 경쟁사 어닝콜에서 언급된 시장 위협
- 규제 기관 문서 (FTC 반독점 조사, EU 규제 동향)
- Glassdoor 직원 리뷰 (내부 문화, 경영진에 대한 실질적 평가)
- 해당 기업의 과거 실패 사례 및 약속 불이행 기록

⚠️ **v2.2 조정:** 위 데이터 파이프라인은 **Phase 3**에서 본격 구축됩니다. Phase 1에서는 Skeptic이 Analyst와 동일한 데이터(10-K Risk Factors + 어닝콜)를 읽지만, **다른 LLM + 구조화된 반론 체크리스트 프롬프트**로 차별화합니다.

**강화 2 — Bull과 Bear에 서로 다른 LLM 사용:**

- Bull 측 (Analyst, Valuer): **Llama 3.1 8B** (로컬)
- Bear 측 (Skeptic): **Qwen 2.5 7B** 또는 **DeepSeek-R1-Distill-Qwen-7B** (로컬, 다른 아키텍처)
- 서로 다른 모델은 서로 다른 편향을 가지고 있어서 실질적 다양성이 생김
- **Phase 1부터 이 원칙이 완전하게 지켜짐** (v2.1은 Phase 2로 이연했으나 v2.2는 Phase 1에 포함)

**강화 3 — 구조화된 반론 체크리스트:**

Skeptic은 반드시 다음 항목을 검토해야 합니다.
- "Bull의 핵심 가정 3가지를 식별하고, 각각이 틀릴 확률과 그 경우의 영향을 정량화하라"
- "이 기업이 5년 내 매출이 50% 이상 감소할 시나리오를 구체적으로 구성하라"
- "현재 밸류에이션에 이미 반영된 낙관적 기대를 식별하라"
- "밸류체인의 가장 취약한 고리를 지목하라" (수동 밸류체인 문서 기반)

**Skeptic의 Python 도구:** `verify_number(claim, source)` — Bull 측이 인용한 수치를 Python이 원본 데이터와 대조하여 정확성을 검증

### 디베이트 컨텍스트 압축

Bull/Bear 디베이트를 3라운드 진행하면, 이전 라운드의 긴 대화 내용이 다음 프롬프트에 계속 누적됩니다. 이로 인해 토큰 사용량이 기하급수적으로 증가합니다.

**대응 전략 — 라운드 간 요약 압축:**

각 디베이트 라운드가 끝나면, 해당 라운드의 전체 내용을 넘기는 것이 아니라 **핵심 논점 요약본**만 다음 라운드에 전달합니다.

- 라운드 1 원문: ~5,000 토큰 → 요약: ~800 토큰
- 라운드 2 원문: ~5,000 토큰 → 요약: ~800 토큰
- 라운드 3에 들어가는 컨텍스트: 라운드 1 요약 + 라운드 2 요약 + 새 데이터 = ~3,000 토큰

요약 작업은 로컬 8B 모델이 수행하여 추가 비용을 발생시키지 않습니다.

**전체 원문은 보존합니다.** 최종 보고서에는 요약이 아닌 원문을 포함하되, 에이전트 간 전달에만 요약을 사용합니다. 사람이 전체 논쟁 과정을 추적할 수 있어야 합니다.

> ⚠️ **Phase 1에서 디베이트 라운드 수:** 구현 부담을 낮추기 위해 **1~2 라운드로 시작**합니다. 3 라운드 풀 디베이트는 Phase 2에서 컨텍스트 압축과 함께 도입.

## 7.5. The Steward — 최종 결정자

> 📌 **구현 순서: Phase 2에서 추가** (MVP에서는 사람이 직접 이 역할)

**역할:** 포트폴리오 매니저. 모든 에이전트의 보고서와 디베이트 기록을 종합하여 최종 판단을 내립니다.

**가장 중요한 규율:** "확신이 없으면 패스한다."

**분석 항목:**

- Bull Thesis vs Bear Thesis 종합 평가
- 리스크/보상 비율
- 포트폴리오 맥락 (집중도, 산업 편중, 환율 노출)
- 포지션 사이징

**Python 도구:** `get_portfolio_summary()`, `calculate_correlation(ticker, portfolio)`, `calculate_position_size(conviction_level, portfolio_value)`

**최종 출력:**

| 판정 | 의미 |
|------|------|
| **BUY** | 매수. 권장 비중과 근거를 함께 제시 |
| **HOLD** | 이미 보유 중이면 계속 보유. 추가 매수는 하지 않음 |
| **PASS** | 매수하지 않음. 이유를 명시 |

---

# 8. 보고서 출력: 결과는 어떻게 받아보는가?

## 8.1. 즉시 알림 (OpenClaw → 텔레그램)

> ⚠️ **Phase 1 주의:** OpenClaw 연동은 Phase 2입니다. Phase 1에서는 로컬 CLI 또는 Markdown 파일 출력으로 시작합니다.

긴급하거나 중요한 결과는 텔레그램으로 즉시 알림이 옵니다(Phase 2 이후).

**알림 예시:**
> **[Tier 1 분석 완료] NVIDIA (NVDA)**
> 판정: HOLD (추가 매수 보류)
> 핵심: Bull 논거 견고하나 현 밸류에이션에서 안전마진 부족.
> Bear 핵심 반론: AI capex 사이클 둔화 시 PER 수축 리스크.
> 전체 보고서: [링크]

## 8.2. 상세 보고서 (Markdown 파일)

보고서에는 다음 내용이 포함됩니다.

- Economist의 매크로 환경 요약 (Phase 2 이후)
- Analyst의 기업 분석 (**모든 수치에 출처 명시, 모든 계산은 Python 도구 실행 결과**)
- Valuer의 멀티플 비교 + 역DCF 결과 (**Python 계산 결과 + LLM 해석 분리 표시**)
- Bull Thesis vs Bear Thesis 전문 (Phase 1은 Analyst 데이터 + 다른 로컬 모델 기반, Phase 3부터 Skeptic 전용 데이터 포함)
- Steward의 최종 판정 및 근거 (Phase 2 이후)
- ⚠️ 데이터 소스 간 불일치가 있었다면 해당 항목 표시
- 밸류체인 관련 주요 변화 사항

## 8.3. 밸류체인 업데이트

- **Phase 1:** 수동 밸류체인 문서는 분석자가 직접 수정
- **Phase 2:** 자동 구축된 그래프가 분석 과정에서 발견된 밸류체인 변화를 자동 반영 (피드백 루프)

---

# 9. 하드웨어 및 비용: 무엇이 필요한가?

## 9.1. 하드웨어

| 장비 | 사양 | 용도 | 비용 |
|------|------|------|------|
| **Mac mini M4 기본형** | 16GB 통합 메모리 | 전체 시스템 실행 | 약 80만원 (1회) |

### 16GB 메모리 관리 전략

Ollama에서 8B 모델을 돌리면서 동시에 ChromaDB, CrewAI, 데이터 수집 파이프라인, SQLite를 모두 돌리면 16GB는 빠듯할 수 있습니다.

### 에이전트 순차 처리 큐

**핵심 원칙:** 여러 에이전트가 동시에 LLM을 호출하지 않도록, **비동기 순차 처리 큐(Queue)**를 구성합니다.

**작동 방식 (v2.2 Phase 1 기준):**

1. 분석 요청이 들어오면 큐에 등록
2. **Llama 3.1 8B 로드** → Analyst 실행 → 결과 저장 (모델은 유지)
3. Valuer 실행 → 결과 저장 → **Llama 언로드**
4. **Qwen 2.5 7B 로드** → Skeptic 실행 → 결과 저장 → **Qwen 언로드**
5. (Phase 2 이후) Steward가 종합 실행

이렇게 하면 어느 시점에서도 LLM 인스턴스 1개만 메모리에 올라가므로, 16GB에서도 안정적으로 운영됩니다.

**모델 스왑 오버헤드:** Llama → Qwen 전환 시 30초~2분. Phase 1은 일 1~3건 분석 빈도라 영향 없음.

**배경 프로세스 분리:**

| 프로세스 | 특성 | 메모리 사용 | 실행 방식 |
|----------|------|-------------|-----------|
| RSS/뉴스 폴링 | 경량 | ~50MB | 상시 실행 |
| 키워드 필터링 | 경량 | ~30MB | 상시 실행 |
| ChromaDB 서버 | 중간 | ~200~500MB | 필요 시 로드 |
| Ollama + 8B 모델 | 무거움 | ~5~6GB | 분석 시에만 로드 |
| CrewAI 오케스트레이션 | 경량 | ~100MB | 분석 시에만 실행 |

### ⚠️ v2.2 수정: 10-K 문서 처리 로컬 전략

v2.1은 10-K 대용량 문서 요약에 Claude Haiku API를 제안했습니다. v2.2는 이를 **로컬 구현 우선**으로 뒤집습니다.

**로컬 우선 전략:**

1. **핵심 섹션 추출 (기본):** 10-K 전문을 넣지 않고, 핵심 섹션(Risk Factors, MD&A, 사업 개요, 재무제표 주석)만 추출하여 로컬 모델에 전달. 대부분의 분석 유스케이스에서 이것으로 충분
2. **Llama 3.1 128K 컨텍스트 활용 (필요 시):** Llama 3.1은 기본 128K 컨텍스트 지원. Ollama에서 `num_ctx=131072`로 설정 가능. 메모리 사용량이 ~10GB로 증가 → 순차 큐로 다른 프로세스 언로드 후 로드
3. **Map-Reduce 방식 (메모리 부담 시):** 10-K를 섹션 단위로 쪼개 각각 요약 후, 요약들을 다시 통합 요약. 전부 Llama 8B 로컬. 메모리 부담 최소
4. **수치 데이터:** 재무제표 수치는 FMP API에서 이미 정제된 형태로 가져오므로, 10-K에서 수치를 직접 추출할 필요 없음

**외부 API는 최후 수단:** 로컬 방식이 품질 부족으로 판명된 경우에만 Phase 3 이후 Claude Haiku API 도입 검토. **필수가 아닌 옵션**입니다.

**업그레이드 시점:** Phase 3 이후 종목 수가 늘어나고 ChromaDB 데이터가 쌓이면, Mac mini M4 Pro 24GB로 업그레이드하거나 일부 서비스를 클라우드로 분리하는 것을 검토합니다. Phase 1~2에서는 16GB로 충분합니다.

## 9.2. 소프트웨어 스택 (전부 무료)

| 소프트웨어 | 역할 | 라이선스/비용 |
|------------|------|---------------|
| **Ollama** | 로컬에서 AI 모델 실행 | 무료 |
| **Llama 3.1 8B** | 사전 필터, 경량 분석, Bull 측 에이전트 (Analyst, Valuer) | 무료 (Meta 오픈소스) |
| **Qwen 2.5 7B** (또는 DeepSeek-R1-Distill-Qwen-7B) | Bear 측 에이전트 (Skeptic) — 로컬 모델 다양성 확보 | 무료 |
| **CrewAI** | 에이전트 오케스트레이션 프레임워크 | 무료 (MIT 라이선스) |
| **ChromaDB** | RAG 벡터 데이터베이스 | 무료 (오픈소스) |
| **SQLite** | 포트폴리오 상태 저장 | 무료 |
| **NetworkX** | 밸류체인 그래프 저장/관리 (Phase 2 이후) | 무료 |
| **OpenClaw** | 메시징 인터페이스 (Phase 2 이후) | 무료 (MIT 라이선스) |
| **Python** | 전체 시스템 개발 언어 | 무료 |

**Phase 3 이후 옵션 (필수 아님):**

| 소프트웨어 | 역할 | 비용 |
|------------|------|------|
| **DeepSeek-R1 (API)** | 로컬 품질 미달 시 Skeptic 승격 (Phase 2+) | 매우 저렴 |
| **Claude Haiku (API)** | 로컬 품질 미달 시 10-K 요약 (Phase 3+) | 저렴 |

## 9.3. 월 운영 비용

### v2.2 비용 추정 (로컬 우선)

| Phase | 월 비용 |
|-------|---------|
| **Phase 1 (완전 로컬)** | **0원** (전력비 제외) |
| Phase 2 (로컬 유지) | 0원 |
| Phase 2 (API 도입 시) | 약 2,000~5,000원 |
| Phase 3 (FMP 유료 플랜 + API 옵션) | 약 20,000~25,000원 |

### 토큰 비용 하드 리밋 (Phase 2 이후 API 도입 시 적용)

API를 도입할 경우, 시스템에 월 예산 상한선을 코드로 설정합니다. 예산이 소진되면 API 호출이 자동 차단되고 알림이 옵니다.

| 항목 | 하드 리밋 |
|------|-----------|
| 월 총 API 비용 상한 | 30,000원 |
| 종목 1개당 심층 분석 토큰 상한 | 50,000 토큰 |
| 일일 심층 분석 건수 상한 | 3건 |
| 밸류체인 연쇄 재분석 일일 상한 | 2건 |

이 하드 리밋은 환경 변수로 설정하여 언제든 조정 가능합니다. **Phase 1은 API를 사용하지 않으므로 이 리밋이 비활성화됩니다.**

## 9.4. 총 예산 정리

| 항목 | 금액 |
|------|------|
| 초기 자본금 | 3,000,000원 |
| Mac mini M4 구입 | -800,000원 |
| **남는 투자 자본** | **2,200,000원** |
| Phase 1 월 운영비 | **0원** |
| Phase 2 월 운영비 (API 도입 시 선택) | 약 2,000~5,000원 |
| Phase 3 월 운영비 (FMP 유료 + API 옵션) | 약 20,000~25,000원 |

---

# 10. 구현 전략: 어떻게 만들 것인가?

## 10.1. 프레임워크 선택: CrewAI

여러 에이전트 프레임워크 중 CrewAI를 선택한 이유:

- **역할 기반 모델링:** 역할을 그대로 CrewAI의 Agent Role로 정의 가능
- **도구(Tool) 통합:** Python 함수를 CrewAI Tool로 등록하여 "LLM은 판단, Python은 계산" 원칙을 자연스럽게 구현
- **로컬 LLM 지원:** Ollama 연동이 공식 지원됨. API 없이도 완전 작동
- **장투 시스템에 적합:** 주 1~2회 깊은 분석이 핵심이므로 성능 한계가 문제되지 않음
- **추후 확장성:** LangGraph로 마이그레이션하는 경로가 잘 정립됨

## 10.2. 구현 단계

### Phase 1 — MVP: 종목 1개 + 3개 에이전트 (5~7주, 완전 로컬)

**목표:** "이 보고서가 정말 내 투자 판단에 도움이 되는가?" + "로컬 모델 다양성의 Skeptic이 실제 가치를 더하는가?" + "밸류체인 문서가 보고서 품질을 개선하는가?"를 검증

**⚠️ v2.2 주요 변경:**
- v2.1 4~6주 → v2.2 5~7주 (수동 밸류체인 문서와 Qwen 모델 통합 작업 추가)
- 외부 LLM API 일체 사용하지 않음. 비용 0원
- Skeptic을 Claude Haiku가 아닌 **Qwen 2.5 7B (로컬)**로 구현

**작업 목록:**

1. **환경 셋업:**
   - 맥미니에 Ollama 설치
   - Llama 3.1 8B + Qwen 2.5 7B (또는 DeepSeek-R1-Distill-Qwen-7B) 다운로드
   - CrewAI, ChromaDB, SQLite, Python 기초 환경 구축

2. **데이터 수집 (FMP 무료 티어 + yfinance):**
   - 기본 재무 데이터 수집 파이프라인
   - SEC EDGAR 텍스트 섹션(Risk Factors, MD&A) 추출 (수치 파싱은 시도하지 않음)
   - **Phase 1 경량 교차검증 구현** (주가/시총/기본 비율만, EDGAR 수치 비교는 Phase 3)

3. **Python 계산 도구 개발:**
   - `calculate_per()`, `calculate_ev_ebitda()`, `get_peer_multiples()`, `reverse_dcf()`, `verify_number()` 등
   - 모든 수치 계산은 Python이 담당, LLM은 호출·해석만

4. **수동 밸류체인 문서 작성:**
   - 분석 대상 종목 1개에 대해 Markdown으로 상류·동급·하류·인프라 기술
   - Analyst 에이전트의 RAG 컨텍스트에 포함

5. **에이전트 프롬프트 작성:**
   - Analyst (Llama 8B), Valuer (Llama 8B), Skeptic (Qwen 7B)
   - Skeptic에 구조화된 반론 체크리스트 프롬프트 적용

6. **에이전트 순차 처리 큐 구현:**
   - Llama 로드 → Analyst → Valuer → Llama 언로드
   - Qwen 로드 → Skeptic → Qwen 언로드
   - 모델 스왑 오버헤드 처리

7. **Phase 1 품질 지표 시스템:**
   - 수치 출처 명시율 자동 측정
   - Python 도구 호출 건수 vs LLM 자체 계산 의심 건수 감사
   - Skeptic이 Bull 주장 중 반론·검증한 비율
   - 보고서당 확정 가정(하드 넘버) 수 vs 시나리오 가정 수

8. **종목 1개 분석 보고서 생성:**
   - 로컬 CLI 또는 Markdown 파일 출력
   - 반복 튜닝을 통해 품질 개선

9. **MVP 평가 4대 질문:**
   1. 보고서가 투자 판단에 실제로 도움이 되는가?
   2. Skeptic의 반론이 로컬 모델 다양성만으로도 Analyst/Valuer와 유의미하게 다른 관점을 제공하는가?
   3. 수동 밸류체인 정보가 보고서 품질을 유의미하게 높였는가?
   4. 품질 지표상 "LLM은 판단, Python은 계산" 원칙이 실제로 지켜지고 있는가?

**이 단계에서 아직 안 만드는 것:** OpenClaw 연동, 실시간 뉴스 모니터링, 자동 밸류체인 그래프, Economist, Steward, 디베이트 3 라운드 풀 루프, 디베이트 컨텍스트 압축, EDGAR XBRL 파싱, 외부 LLM API

### Phase 2 — 핵심 기능 확장 (6~10주)

Phase 1의 4대 질문에서 긍정 판정을 받은 후에만 진행합니다.

1. **Economist, Steward 에이전트 추가** (5개 에이전트 완성)
2. **Bull/Bear 디베이트 3 라운드 풀 루프 구현** + 디베이트 컨텍스트 압축
3. **자동 밸류체인 그래프 구축** (핵심 종목 3~5개, Phase 1 수동 문서를 정답지로 활용)
4. **사전 필터 1단계 구현** (키워드 매칭)
5. **OpenClaw 연동** (텔레그램 알림)
6. **보고서 자동 생성 + Notion 연동**
7. **FRED API 연동** (매크로 데이터, Economist용)
8. **로컬 모델 품질 재평가:**
   - Phase 1 품질 지표가 기준 미달이면 로컬 모델 업그레이드 (Qwen 14B, Llama 3.3 70B 양자화)
   - 로컬 업그레이드로도 부족하면 **그때 비로소** DeepSeek-R1 API를 Skeptic 한정으로 도입 검토
   - **토큰 비용 하드 리밋 시스템**은 API 도입 시점에 활성화

### Phase 3 — 자동화 및 확장 (10~16주)

1. **3-Tier 종목 관리 시스템** 구현
2. **밸류체인 자동 업데이트 + 연쇄 알림** + 연쇄 비용 통제 규칙
3. **SEC EDGAR XBRL 파싱 파이프라인 구축:**
   - edgartools 등 전문 라이브러리 활용
   - FMP↔EDGAR 정밀 교차검증 활성화
   - EDGAR를 FMP 서비스 중단 시의 최종 대체재로 위상 확보
4. **뉴스 RSS 크롤링 자동화**
5. **사전 필터 2~3단계** (밸류체인 키워드 매칭 + 로컬 AI 의미 필터링)
6. **지정학 리스크 에이전트 강화** (GPR Index 연동)
7. **10-K 대용량 문서 하이브리드 처리:**
   - 1순위: Llama 128K 컨텍스트 또는 Map-Reduce (로컬)
   - 2순위 (필요 시): Claude Haiku API — **옵션이며 필수 아님**
8. **FMP 유료 플랜 업그레이드 필요성 평가**
9. **하드웨어 업그레이드 필요성 평가** (Mac mini M4 Pro 24GB)

### Phase 4 — 검증 및 안정화 (지속적)

- 페이퍼 트레이딩(가상 매매)으로 최소 3~6개월 검증
- 에이전트 프롬프트 지속적 튜닝
- 밸류체인 그래프 정확도 검증 및 수동 보정
- 품질 지표 회귀 테스트 (과거 분석 재실행으로 튜닝의 실질 개선 효과 측정)
- ChromaDB 가비지 컬렉션 루틴 안정화
- 데이터 소스 안정성 모니터링
- yfinance 중단 시 대체 전환 절차 문서화
- **백업 전략:** Mac mini SSD 주간 백업 (Time Machine 또는 외장 SSD) — ChromaDB/SQLite/분석 이력 소실 방지

### 전체 타임라인 요약

| Phase | 기간 | 핵심 산출물 | 비용 |
|-------|------|-------------|------|
| Phase 1 (MVP, 완전 로컬) | 5~7주 | 종목 1개 분석 보고서 + Python 계산 도구 + 수동 밸류체인 + 품질 지표 | **0원** |
| Phase 2 (핵심 기능) | 6~10주 | 5개 에이전트 + 자동 밸류체인 + OpenClaw + 디베이트 풀 루프 | 0~5,000원/월 |
| Phase 3 (자동화) | 10~16주 | 3-Tier + 실시간 모니터링 + EDGAR 파싱 + 하이브리드 처리 | ~25,000원/월 |
| Phase 4 (검증) | 지속적 | 페이퍼 트레이딩 + 튜닝 + 회귀 테스트 | 동일 |
| **쓸 만한 MVP까지** | **약 5~7주** | **(비용 0원)** | |
| **안정적 운영까지** | **약 6개월+** | | |

---

# 11. 중요한 주의사항

## 11.1. AI의 한계

- **AI는 미래를 예측할 수 없습니다.** 주가가 오를지 내릴지 맞추는 수정구슬이 아닙니다.
- **환각(Hallucination) 위험:** "LLM은 판단, Python은 계산" 원칙과 교차검증 파이프라인으로 대응하지만, 완전한 제거는 불가능합니다. 모든 보고서의 수치를 원본과 대조하는 습관이 필요합니다.
- **DCF의 구조적 한계:** 정방향 DCF는 참고용으로만 사용합니다.
- **디베이트의 한계:** 로컬 모델 다양성 + 전용 데이터로 완화하지만, 최종적으로 사람의 비판적 사고가 필수입니다.
- **블랙스완 이벤트:** 예측 불가능한 사건에는 어떤 시스템도 대비할 수 없습니다.
- **데이터 품질:** 무료/저비용 소스는 기관급 데이터와 품질 차이가 존재합니다. 교차검증으로 완화하지만 근본적 한계는 인정합니다.
- **yfinance 중단 리스크:** 비공식 스크래핑 기반이므로 언제든 작동 중단 가능합니다. FMP를 주력으로 전환한 이유입니다.
- **FMP 의존 리스크:** FMP도 외부 상용 서비스이므로 가격 인상·서비스 종료 리스크가 있습니다. Phase 3에서 EDGAR XBRL 파싱을 최종 대체재로 확보합니다.
- **로컬 모델 품질 한계(v2.2 신규):** Llama 3.1 8B, Qwen 2.5 7B의 금융 해석 품질은 GPT-4/Claude Opus 수준에 미치지 못할 수 있습니다. Phase 1 품질 지표로 측정하고, 부족하면 Phase 2에서 단계적 에스컬레이션(더 큰 로컬 모델 → API 제한적 도입)합니다.

## 11.2. API 도입 철학 (v2.2 신규)

이 시스템은 **로컬 우선, API 최후** 원칙을 따릅니다.

- **Phase 1:** 외부 LLM API 일체 사용하지 않음. 비용 0원
- **Phase 2+:** 로컬 품질 지표가 미달인 경우에만, 미달한 영역에 한정하여 API 도입 검토
- API는 "편리하니까" 도입하는 것이 아니라 "로컬로 안 되니까" 도입하는 것

이 원칙의 목적은:
- 지속 가능한 운영비 구조 확보 (API 가격 변동 리스크 제거)
- 데이터 주권 유지 (외부 서비스에 분석 프롬프트·데이터 유출 최소화)
- 학습 효과 (로컬 환경 관리 역량 축적)

## 11.3. 법적 고려사항

- 이 시스템은 **개인 용도**로만 사용해야 합니다. 투자자문업 미등록 상태에서 타인 대상 서비스 제공은 자본시장법 위반입니다.
- 주식 모임 카톡방 등 **타인이 열람 가능한 채널에 분석 결과를 공유하는 행위는 "개인 용도"의 경계를 벗어날 수 있습니다.** OpenClaw 시나리오에서 이 점을 유의하십시오. 분석 결과의 공유 범위는 본인 열람으로 제한하는 것을 권장합니다.
- 미국 주식 양도소득세: 연 250만원 기본 공제 후 22%.
- 데이터 크롤링 시 각 사이트의 이용약관 준수 필수.

## 11.4. 올바른 기대치

이 시스템의 가치는 "돈을 벌어주는 봇"이 아닙니다.

- **투자 리서치 프로세스를 체계화**합니다.
- **확증 편향을 줄여줍니다.**
- **정보 처리 능력을 증폭합니다.**
- **밸류체인 사고를 자동화합니다.**

Two Sigma의 Matt Greenwood가 말했듯이, "미래는 AI가 인간을 대체하는 것이 아니라, AI를 잘 쓰는 인간이 그렇지 못한 인간을 대체하는 것"입니다.

---

# 12. 용어 사전

| 용어 | 설명 |
|------|------|
| **에이전트(Agent)** | 특정 역할과 목표를 가진 AI 프로그램 |
| **멀티 에이전트 시스템** | 여러 에이전트가 협업하여 복잡한 문제를 해결하는 구조 |
| **LLM (Large Language Model)** | ChatGPT, Claude 같은 대규모 언어 모델 |
| **Ollama** | 맥/PC에서 AI 모델을 무료로 실행할 수 있게 해주는 소프트웨어 |
| **Llama 3.1 8B** | Meta가 개발한 오픈소스 LLM. 범용 추론에 균형 잡힘 |
| **Qwen 2.5 7B** | Alibaba가 개발한 오픈소스 LLM. Llama와 독립적 훈련 데이터 |
| **DeepSeek-R1-Distill** | 반박·추론 능력에 특화된 증류 모델 |
| **CrewAI** | 여러 AI 에이전트를 팀처럼 조직하는 오픈소스 프레임워크 |
| **Tool (도구)** | 에이전트가 호출할 수 있는 Python 함수. 계산, 데이터 조회 등을 정확하게 수행 |
| **RAG** | Retrieval-Augmented Generation. AI가 문서에서 관련 자료를 찾아 읽고 답하는 기술 |
| **ChromaDB** | 문서를 의미 기반으로 검색할 수 있게 저장하는 벡터 데이터베이스 |
| **DCF** | Discounted Cash Flow. 기업의 미래 현금흐름을 현재 가치로 환산하는 가치평가 방법 |
| **역DCF** | 현재 주가에서 시장이 가정하고 있는 성장률을 역산하는 방법 |
| **멀티플** | PER, EV/EBITDA 등 기업 가치를 이익의 배수로 표현한 것 |
| **Moat (경쟁 우위)** | 기업이 경쟁사로부터 자신을 보호하는 구조적 강점 |
| **안전마진** | 내재가치 대비 충분히 싸게 살 때의 할인폭 |
| **밸류체인** | 하나의 제품이 만들어지기까지 연결된 기업들의 관계망 |
| **레드팀** | 의도적으로 반대 입장에서 논거를 공격하는 팀 |
| **확증 편향** | 자기가 믿고 싶은 정보만 수집하는 심리적 경향 |
| **환각(Hallucination)** | AI가 존재하지 않는 정보를 그럴듯하게 만들어내는 현상 |
| **교차검증** | 같은 데이터를 2개 이상의 소스에서 확인하여 정확성을 높이는 방법 |
| **하드 리밋** | 시스템이 절대 초과하지 못하도록 코드로 강제하는 비용/사용량 상한선 |
| **순차 처리 큐** | 작업을 한 번에 하나씩 순서대로 처리하는 구조. 메모리 사용을 통제 |
| **컨텍스트 윈도우** | AI 모델이 한 번에 읽을 수 있는 텍스트의 최대 길이 |
| **모델 스왑** | 순차 큐에서 다른 모델을 쓰기 위해 기존 모델을 메모리에서 내리고 새 모델을 올리는 작업 |
| **Map-Reduce** | 대용량 문서를 쪼개 부분 요약한 뒤 다시 통합 요약하는 전략 |
| **XBRL** | 재무 데이터 교환을 위한 표준 태그 언어. SEC Filing에 사용 |
| **진실의 근원(Ground Truth)** | 다른 모든 데이터 소스가 의심스러울 때 최종 기준이 되는 원본 데이터 |
| **Go/No-Go 게이트** | 다음 단계로 진행할지 중단할지 판단하는 공식 체크포인트 |
| **OpenClaw** | 오픈소스 AI 개인 비서 |
| **API** | 프로그램 간 데이터를 주고받는 통로 |
| **FMP (Financial Modeling Prep)** | 안정적인 재무 데이터 API 서비스 |
| **RSS** | 웹사이트의 새 글을 자동으로 받아보는 구독 기술 |
| **SQLite** | 파일 하나로 작동하는 가벼운 데이터베이스 |
| **NetworkX** | Python에서 네트워크 구조를 다루는 라이브러리 |
| **페이퍼 트레이딩** | 실제 돈 없이 가상으로 매매를 시뮬레이션하는 것 |
| **토큰** | AI가 텍스트를 처리하는 단위 |
| **가비지 컬렉션** | 불필요한 데이터를 정리하여 저장 공간을 확보하는 작업 |
| **회귀 테스트(Regression Test)** | 변경 후에도 기존 동작이 유지되는지 확인하는 자동화 테스트 |

---

# 부록 A: 버전 변경 이력

## v1.0 → v2.0 변경 (1차 리뷰 반영)

| 항목 | v1.0 | v2.0 | 변경 사유 |
|------|------|------|-----------|
| 데이터 품질 | "모든 소스 무료"로 낙관적 기술 | 무료 소스의 한계 명시 + 교차검증 추가 | 무료 데이터의 부정확성/지연 |
| 가치평가 | DCF 주력 | 멀티플 주력 + 역DCF 보조 + DCF 참고용 | LLM의 DCF 가정 설정 한계 |
| 디베이트 | 같은 LLM이 양쪽 | 다른 LLM + 전용 데이터 + 체크리스트 | 동일 모델의 자기 반박 한계 |
| 사전 필터 | 8B 모델 의미 필터링 | 3단계 점진적 접근 | 2~3차 연결 판단 난이도 |
| 하드웨어 | 16GB 충분 | 메모리 관리 전략 + 업그레이드 시점 명시 | 동시 실행 시 메모리 부족 |
| 구축 일정 | 전체 8주 | MVP 4~6주, 전체 6개월+ | 구현 난이도 과소평가 |
| 구현 전략 | 전체를 한 번에 | 종목 1개 + 3개 에이전트 MVP 먼저 | 검증 우선 |

## v2.0 → v2.1 변경 (2차 리뷰 반영)

| 항목 | v2.0 | v2.1 | 변경 사유 |
|------|------|------|-----------|
| 데이터 소스 전략 | yfinance 주력 + 교차검증 | FMP 주력 + yfinance 백업 + EDGAR 원본 | yfinance는 비공식 스크래핑 기반, 언제든 중단 가능 |
| EDGAR 파싱 | 난이도 미언급 | 파싱 복잡성 명시 + 핵심 섹션만 추출 전략 | HTML/XBRL 정제의 실제 난이도 |
| LLM/계산 역할 | 구분 불명확 | "LLM은 판단, Python은 계산" 원칙 명시 | LLM의 수치 계산 환각 리스크 |
| 에이전트 실행 | 동시 실행 가정 | 순차 처리 큐 도입 | 16GB 메모리에서 OOM 방지 |
| 10-K 처리 | 로컬 모델로 전부 처리 | 하이브리드 (핵심 섹션 로컬 + 전문 요약 API) | 8B 모델의 컨텍스트 윈도우 한계 |
| 비용 추정 | 월 1,000~5,000원 | 월 2,500~25,000원 (FMP 플랜에 따라) | 토큰 누적, 연쇄 분석, 10-K 요약 비용 |
| 비용 통제 | 언급 없음 | 하드 리밋(월 상한, 종목당 상한, 일일 상한) | API 비용 폭탄 방지 |
| 디베이트 토큰 | 원문 누적 전달 | 라운드 간 요약 압축 (원문은 보고서에 보존) | 토큰 기하급수 증가 방지 |
| 밸류체인 연쇄 | 무제한 트리거 | 일일 3건 + 연쇄 깊이 2단계 제한 | 연쇄 비용 통제 |
| 유료 데이터 | 전부 무료 | FMP 유료 플랜($14/월) 옵션 추가 | 데이터 안정성 보험 |

## v2.1 → v2.2 변경 (3차 리뷰 반영)

| 항목 | v2.1 | v2.2 | 변경 사유 |
|------|------|------|-----------|
| Phase 1 LLM 전략 | Llama 8B 전체 (Skeptic 포함) | Llama 3.1 8B + Qwen 2.5 7B 로컬 다양성 | Phase 1에서부터 "다른 LLM" 원칙 확보, 동시에 API 비용 0원 유지 |
| Phase 1 비용 | 약 2,500~7,000원/월 | **0원** | 로컬 우선 원칙 도입 |
| Skeptic 차별화 시점 | Phase 2에서 다른 LLM 도입 | **Phase 1부터** 로컬 모델 다양성 확보 | Skeptic 실효성을 MVP 단계부터 검증해야 Go/No-Go 판단 가능 |
| 10-K 요약 전략 | Claude Haiku API | 로컬 Llama 128K 컨텍스트 또는 Map-Reduce 우선 | API 비용 회피, 외부 의존 감소 |
| API 도입 철학 | 기본값으로 전제 | **최후 수단**으로 재정의 | 로컬 실패 시에만 API 검토 |
| Phase 1 EDGAR 교차검증 | FMP↔EDGAR 수치 교차검증 포함 | **Phase 3으로 이연** (EDGAR 수치 파싱은 난이도 高) | v2.1 자체가 인정한 파싱 난이도를 Phase 1 범위와 일치시킴 |
| Phase 1 교차검증 | 미축소 | 경량 버전 (주가/시총/기본 비율만) | 현실적 구현 가능 범위로 축소 |
| EDGAR 위상 | 교차검증용 원본 | **FMP 서비스 중단 시의 최종 대체재**로 전략적 승격 | FMP 단일 지점 장애(SPOF) 대응 |
| Phase 1 밸류체인 | 미포함 (Phase 2로 이연) | **수동 밸류체인 문서 포함** | 밸류체인 없는 MVP로는 Go/No-Go 판단 오염 |
| 품질 측정 지표 | 없음 | **Phase 1부터 4대 지표 도입** | 주관적 튜닝 방지, 객관적 Go/No-Go 근거 확보 |
| Phase 1 기간 | 4~6주 | 5~7주 | 수동 밸류체인 + Qwen 통합 작업 반영 |
| Phase 1 디베이트 | 풀 3라운드 상정 | 1~2 라운드로 축소 (풀 루프는 Phase 2) | Phase 1 구현 부담 완화 |
| 법적 고려사항 | "개인 용도"만 명시 | 카톡방 공유의 경계 위험 명시 | v2.1 §2의 OpenClaw 시나리오와 §11.2의 잠재적 충돌 해소 |
| 백업 전략 | 언급 없음 | Mac mini SSD 주간 백업 명시 | 단일 기기 의존의 재해 복구 |
| 회귀 테스트 | 언급 없음 | 품질 지표 회귀 테스트 Phase 4에 명시 | 튜닝의 실질 개선 효과 측정 |

---

> **문서 버전:** v2.2
> **작성일:** 2026년 4월
> **상태:** 설계 단계 (구현 전)
> **핵심 철학:** 로컬 우선, API 최후
> **다음 단계:** Phase 1 MVP — 종목 1개 + Analyst(Llama 8B) + Valuer(Llama 8B) + Skeptic(Qwen 7B) + 수동 밸류체인 + 품질 지표
